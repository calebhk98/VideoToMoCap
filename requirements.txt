# Runtime deps for the orchestration layer (Pipeline 1, GPU-free parts).
numpy>=1.21
pyyaml>=5.4        # only needed for load_config(); code paths work without it

# NOTE: the HMR backends (GVHMR/WHAM/TRAM) and the motion model (MDM) each have
# their own heavy environments (torch, CUDA, SMPL/SMPL-X body models, SLAM deps).
# Install those from the upstream repos into dedicated envs; do NOT mix them into
# this one. See README.md "Installing a backend".
